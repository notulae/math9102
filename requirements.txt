# Minimum versions. `pip install .` reads these from pyproject.toml,
# so you do not normally need this file.
# For the exact versions the material was built against, see
# requirements-exact.txt - but expect it to resolve only on Linux with
# Python 3.12.
numpy>=1.26
pandas>=2.2
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
statsmodels>=0.14
pingouin>=0.5.4
scikit-learn>=1.4
factor_analyzer>=0.5
pyreadstat>=1.2
openpyxl>=3.1
jupyterlab>=4.0
ipykernel>=6.29
